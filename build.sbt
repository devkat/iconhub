organization in ThisBuild := "net.iconhub"

name in ThisBuild         := "iconhub"

version in ThisBuild      := "1.0-SNAPSHOT"

scalaVersion in ThisBuild := "2.9.1"

scanDirectories in Compile := Nil

